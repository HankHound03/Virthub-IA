services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-8000}:80"
    env_file:
      - .env
    environment:
      DB_CONNECTION: mariadb
      DB_HOST: mariadb
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-virthub}
      DB_USERNAME: ${DB_USERNAME:-virthub}
      DB_PASSWORD: ${DB_PASSWORD:-virthub_password}
    depends_on:
      mariadb:
        condition: service_healthy
    volumes:
      - app_storage:/var/www/html/storage
    networks:
      - virthub

  mariadb:
    image: mariadb:11.4
    environment:
      MARIADB_DATABASE: ${DB_DATABASE:-virthub}
      MARIADB_USER: ${DB_USERNAME:-virthub}
      MARIADB_PASSWORD: ${DB_PASSWORD:-virthub_password}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root_password}
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -h localhost -u root -p$${MARIADB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - virthub

volumes:
  app_storage:
  mariadb_data:

networks:
  virthub:
    driver: bridge